/**
 * Default example files loaded on first visit (when IndexedDB is empty).
 * Paths are relative to `public/` — Vite serves them as static assets.
 */
export const DEFAULT_EXAMPLES = [
  'examples/chord-progression.mid',
  'examples/c-major-scale.mid',
  'examples/c-major-scale.musicxml',
] as const
